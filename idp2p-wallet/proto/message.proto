syntax = "proto3";
package idp2p.pb;

message Idp2pPubsubMessage {
    oneof pubsub_message_kind {
       string resolve_request = 1; // 
       bytes resolve_response = 2;
       bytes oneshot_message = 3;
    }
}