package idp2p:id;

world idp2p-id {

    variant id-verifier-error {
        other(string)
    }

    export verify-inception: func(inception: list<u8>) -> result<list<u8>, id-verifier-error>;

    export verify-event: func(view: list<u8>, event: list<u8>) -> result<list<u8>, id-verifier-error>;
}