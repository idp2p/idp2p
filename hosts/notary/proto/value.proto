syntax = "proto3";
package idp2p.pb;

message KeyValue {
    bytes key = 1; // Can be an identity, credential or contract identifier
    bytes value = 2; // Can be an identity event, credential event or contract body
}

message Batch {
    repeated KeyValue values = 1;
}

message Block {
    repeated bytes batch = 1;
}
